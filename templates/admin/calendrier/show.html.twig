{% extends 'base.html.twig' %}

{% block title %}Calendrier{% endblock %}

{% block body %}
    <h1>Calendrier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ calendrier.id }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ calendrier.dateDebut }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ calendrier.dateFin }}</td>
            </tr>
            <tr>
                <th>HeureDebut</th>
                <td>{{ calendrier.heureDebut }}</td>
            </tr>
            <tr>
                <th>HeureFin</th>
                <td>{{ calendrier.heureFin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('calendrier_index') }}">back to list</a>

    <a href="{{ path('calendrier_edit', {'id': calendrier.id}) }}">edit</a>

    {{ include('calendrier/_delete_form.html.twig') }}
{% endblock %}
